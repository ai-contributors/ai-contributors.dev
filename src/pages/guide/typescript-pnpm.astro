---
import DocLayout from '../../layouts/DocLayout.astro';
import { renderUpstreamMd } from '../../lib/render-upstream-md';

const upstreamPath = 'AI-CONTRIBUTOR-GUIDE.md';
const { html, title } = renderUpstreamMd(upstreamPath);
---

<DocLayout title={title} crumb="adoption · TypeScript + pnpm" upstreamPath={upstreamPath}>
  <Fragment set:html={html} />
</DocLayout>
