---
import DocLayout from '../../layouts/DocLayout.astro';
import { renderUpstreamMd } from '../../lib/render-upstream-md';

const upstreamPath = 'skills/ai-contributor-audit/README.md';
const { html, title } = renderUpstreamMd(upstreamPath);
---

<DocLayout title={title} crumb="skills · audit" upstreamPath={upstreamPath}>
  <Fragment set:html={html} />
</DocLayout>
